/* This should throw a syntactic error because the while loop on line 9 contains empty parameters instead of the keyword void. */

void main(void) {
  int x; 
  int fac;
  x = input();
  fac = 1;
  while () {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}
