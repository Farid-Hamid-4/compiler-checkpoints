/* A program to perform Euclid's algorithm to
   compute the greatest common divisor called gcd */

int gcd(int u, int v) {
  bool k;
  if (v == 0) return u;
  else return gcd(v, u - u/k*v);
}

void main(void) {
  int x;  
  int y;

  x = input();
  y = input();
  output(gcd(x, y));
}
